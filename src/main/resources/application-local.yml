spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/corunner_plan
    username: corunner
    password: corunner_secret
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
