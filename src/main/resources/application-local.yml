spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/corruner_plan
    username: corruner
    password: corruner_secret
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
